{
  "name": "oucaaqui-frontend",
  "version": "1.0.0",
  "description": "Ouça Aqui - O Melhor da Música Você Encontra Aqui",
  "main": "server.js",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd .. && rm -rf build && mkdir -p build && cp -r frontend/build/* build/",
    "start": "node server.js",
    "dev": "cd frontend && npm start"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0",
    "express": "^4.18.2"
  }
}
