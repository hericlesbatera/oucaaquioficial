{
  "name": "musicasua",
  "version": "1.0.0",
  "description": "Ouça Aqui - O Melhor da Música Você Encontra Aqui",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build 2>&1; mkdir -p ../backend/public && cp -r build/* ../backend/public/ 2>/dev/null || true"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/filesystem": "^8.0.0",
    "@capacitor/preferences": "^8.0.0",
    "@capacitor-community/http": "^1.5.6",
    "@supabase/supabase-js": "^2.86.2"
  }
}
